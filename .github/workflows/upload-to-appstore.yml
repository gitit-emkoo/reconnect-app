name: Build and Upload to App Store Connect

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'
    
    - name: Install EAS CLI
      run: npm install -g eas-cli@latest
    
    - name: Check EAS CLI version
      run: eas --version
    
    - name: Login to Expo
      run: |
        echo "Token length: ${#EXPO_TOKEN}"
        echo "Token starts with: ${EXPO_TOKEN:0:10}..."
        # Try EAS login with token
        eas login || echo "Login failed with exit code: $?"
        eas whoami || echo "Whoami failed with exit code: $?"
        eas project:list || echo "Project list failed with exit code: $?"
      env:
        EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    
    - name: Initialize EAS project
      run: eas init --non-interactive
      env:
        EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    
    - name: Check project status
      run: eas project:info
      env:
        EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    
    - name: Build iOS
      run: eas build --platform ios --profile production --non-interactive
      env:
        EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    
    - name: Upload to App Store Connect
      run: eas submit --platform ios --latest
      env:
        APPLE_ID: ${{ secrets.APPLE_ID }}
        APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }} 